import React, { FC } from "react";
import { Button, Container } from "./style";

const Home: FC = () => {
  return (
    <Container>
      <h2>Game Online</h2>
      <Button>Play</Button>
    </Container>
  );
};

export default Home;
