import React, { FC } from "react";
import { Button, Container } from "./style";

const Home: FC = () => {
  return (
    <Container>
      <Button><h4>Play</h4></Button>
    </Container>
  );
};

export default Home;
